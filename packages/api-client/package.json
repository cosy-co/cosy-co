{
  "name": "@cosy-co/api-client",
  "version": "0.0.0",
  "description": "A client that allows the frontend to interact with the backend",
  "main": "index.js",
  "scripts": {
    "build": "tsup src/index.ts --format cjs --dts",
    "dev": "tsup src/index.ts --format cjs --dts --watch",
    "lint": "eslint \"src\""
  },
  "dependencies": {
    "axios": "^1.4.0"
  },
  "devDependencies": {
    "eslint-config-custom": "workspace:*",
    "tsup": "^7.1.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
